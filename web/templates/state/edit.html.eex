<h2>Edit state</h2>

<%= render "form.html", changeset: @changeset, conn: @conn, user: @user,
                        action: state_path(@conn, :update, @state) %>

<%= link "Back", to: state_path(@conn, :index) %>

<h2>New role</h2>

<%= render "form.html", changeset: @changeset, conn: @conn, user: @user,
                        action: role_path(@conn, :create) %>

<%= link "Back", to: role_path(@conn, :index) %>
